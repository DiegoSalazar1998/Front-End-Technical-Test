import weatherAction from "./weather/weatherAction";
import locationAction from "./location/locationAction";

const actions = {
    weather: weatherAction,
    location: locationAction
};

export default actions;
